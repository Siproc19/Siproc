<?xml version="1.0" encoding="UTF-8"?>
<templates xml:space="preserve">
    <t t-name="siproc_delivery_logistics.DeliveryMapAction">
        <div class="o_action" style="height:100%; display:flex; flex-direction:column; gap:12px; padding:16px; background:#f6f7f9;">
            <div style="display:flex; justify-content:space-between; align-items:center; gap:12px; flex-wrap:wrap;">
                <div>
                    <div style="font-size:24px; font-weight:700;">Mapa de reparto</div>
                    <div style="color:#6b7280;">Visualiza la ruta, la dirección y la ubicación real desde el teléfono.</div>
                </div>
                <div style="display:flex; gap:8px; flex-wrap:wrap;">
                    <button class="btn btn-primary" t-on-click="takeLocationNow">Tomar ubicación</button>
                    <button class="btn btn-secondary" t-on-click="focusCurrentRoute">Centrar ruta</button>
                    <button class="btn btn-success" t-on-click="startTracking" t-if="!state.tracking">Iniciar rastreo</button>
                    <button class="btn btn-warning" t-on-click="stopTracking" t-if="state.tracking">Detener rastreo</button>
                    <button class="btn btn-secondary" t-on-click="openGoogleMaps">Google Maps</button>
                    <button class="btn btn-secondary" t-on-click="openWaze">Waze</button>
                </div>
            </div>

            <div style="display:grid; grid-template-columns: 360px 1fr; gap:16px; min-height:620px;">
                <div style="background:white; border-radius:14px; padding:12px; overflow:auto; box-shadow:0 1px 4px rgba(0,0,0,.08);">
                    <div style="font-weight:700; margin-bottom:8px;">Paradas de la ruta</div>
                    <div t-if="!state.points.length" style="color:#6b7280;">No hay puntos de entrega cargados.</div>
                    <t t-foreach="state.points" t-as="point" t-key="point.id">
                        <button t-on-click="() => this.selectPoint(point, state.points.indexOf(point))"
                                style="width:100%; text-align:left; border:1px solid #e5e7eb; background:#fff; border-radius:12px; padding:12px; margin-bottom:10px; cursor:pointer;">
                            <div style="display:flex; justify-content:space-between; gap:8px; align-items:center;">
                                <strong><t t-esc="point.sequence"/>. <t t-esc="point.name"/></strong>
                                <span style="font-size:12px; padding:4px 8px; border-radius:999px; background:#f3f4f6;"><t t-esc="point.status"/></span>
                            </div>
                            <div style="font-size:13px; color:#4b5563; margin-top:6px;"><t t-esc="point.address"/></div>
                            <div style="font-size:12px; color:#6b7280; margin-top:4px;">
                                <t t-esc="point.zone"/> <t t-if="point.zone and point.municipality">•</t>
                                <t t-esc="point.municipality"/> <t t-if="point.municipality and point.department">•</t>
                                <t t-esc="point.department"/>
                            </div>
                            <div t-if="point.reference" style="font-size:12px; color:#6b7280; margin-top:4px;">Ref: <t t-esc="point.reference"/></div>
                        </button>
                    </t>
                </div>

                <div style="background:white; border-radius:14px; padding:12px; box-shadow:0 1px 4px rgba(0,0,0,.08); display:flex; flex-direction:column; gap:12px;">
                    <div style="display:flex; justify-content:space-between; flex-wrap:wrap; gap:8px;">
                        <div>
                            <div style="font-weight:700; font-size:18px;"><t t-esc="state.route.name || 'Ruta'"/></div>
                            <div style="color:#6b7280;">Estado: <t t-esc="state.route.state || 'draft'"/></div>
                        </div>
                        <div style="font-size:13px; color:#6b7280;">
                            Lat: <t t-esc="state.route.current_latitude || 0"/> | Lon: <t t-esc="state.route.current_longitude || 0"/>
                        </div>
                    </div>
                    <iframe t-att-src="state.mapUrl"
                            style="border:0; width:100%; min-height:520px; border-radius:12px; background:#eef2f7;"
                            loading="lazy"/>
                </div>
            </div>
        </div>
    </t>
</templates>
