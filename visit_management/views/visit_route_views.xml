<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <record id="view_visit_route_form" model="ir.ui.view">
        <field name="name">visit.route.form</field>
        <field name="model">visit.route</field>
        <field name="arch" type="xml">
            <form>
                <header>
                    <button name="action_confirm" string="Confirmar"
                            type="object" class="btn-primary"
                            invisible="state != 'draft'"/>
                    <button name="action_start" string="Iniciar Ruta"
                            type="object" class="btn-primary"
                            invisible="state != 'confirmed'"/>
                    <button name="action_done" string="Completar"
                            type="object" class="btn-success"
                            invisible="state != 'in_progress'"/>
                    <button name="action_cancel" string="Cancelar"
                            type="object"
                            invisible="state in ('done','cancelled')"/>
                    <button name="action_open_map" string="Ver Ruta en Mapa"
                            type="object"/>
                    <field name="state" widget="statusbar"
                           statusbar_visible="draft,confirmed,in_progress,done"/>
                </header>
                <sheet>
                    <div class="oe_title">
                        <h1><field name="name"/></h1>
                    </div>
                    <group>
                        <group>
                            <field name="employee_id"/>
                            <field name="route_date"/>
                        </group>
                        <group>
                            <field name="total_visits"/>
                            <field name="completed_visits"/>
                            <field name="total_km"/>
                        </group>
                    </group>
                    <group>
                        <field name="notes"/>
                    </group>
                    <field name="line_ids">
                        <list editable="bottom">
                            <field name="sequence" widget="handle"/>
                            <field name="visit_id"/>
                            <field name="partner_id"/>
                            <field name="visit_state"/>
                            <field name="estimated_km"/>
                            <field name="estimated_time"/>
                            <field name="notes"/>
                        </list>
                    </field>
                </sheet>
                <chatter/>
            </form>
        </field>
    </record>

    <record id="view_visit_route_list" model="ir.ui.view">
        <field name="name">visit.route.list</field>
        <field name="model">visit.route</field>
        <field name="arch" type="xml">
            <list decoration-success="state == 'done'"
                  decoration-warning="state == 'in_progress'">
                <field name="name"/>
                <field name="route_date"/>
                <field name="employee_id"/>
                <field name="state"/>
                <field name="total_visits"/>
                <field name="completed_visits"/>
                <field name="total_km"/>
            </list>
        </field>
    </record>

    <record id="action_visit_route" model="ir.actions.act_window">
        <field name="name">Rutas Diarias</field>
        <field name="res_model">visit.route</field>
        <field name="view_mode">list,form</field>
    </record>

</odoo>
